---
phase: 02-configuration-foundation
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/config/watcher.py
  - src/config/__init__.py
  - tests/test_config_watcher.py
  - requirements.txt
autonomous: true
requirements:
  - CFG-07

must_haves:
  truths:
    - "Modifying config.yaml triggers reload callback within 2 seconds"
    - "Rapid file saves (editor atomic writes) produce only one reload per edit"
    - "Invalid config during reload logs error but keeps watcher running"
    - "Watcher can be started and stopped cleanly"
  artifacts:
    - path: "src/config/watcher.py"
      provides: "ConfigWatcher with debouncing and start/stop lifecycle"
      exports: ["ConfigWatcher", "start_config_watcher"]
    - path: "tests/test_config_watcher.py"
      provides: "Watcher integration tests for CFG-07"
  key_links:
    - from: "src/config/watcher.py"
      to: "src/config/loader.py"
      via: "reload_callback calls load_config"
      pattern: "reload_callback"
    - from: "src/config/watcher.py"
      to: "watchdog.observers.Observer"
      via: "OS-native file monitoring"
      pattern: "from watchdog"
    - from: "src/config/__init__.py"
      to: "src/config/watcher.py"
      via: "re-exports ConfigWatcher, start_config_watcher"
      pattern: "from .watcher import"
---

<objective>
Implement config file hot-reload using watchdog with debouncing, so the service picks up config changes without restart.

Purpose: CFG-07 requires the system to re-read config when the file changes. This enables users to tweak GPIO pin, cooldown, or source folder on a running service without SSH restart — critical for a headless Pi deployment.

Output: `src/config/watcher.py` with ConfigWatcher class, full test coverage, watchdog added to requirements.txt.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-foundation/02-RESEARCH.md
@.planning/phases/02-configuration-foundation/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From src/config/__init__.py:
```python
from .schema import PrinterConfig
from .loader import load_config, ConfigError
```

From src/config/schema.py:
```python
@dataclass
class PrinterConfig:
    gpio_pin: int = Field(default=17, ge=2, le=27)
    trigger_mode: Literal["press", "switch"] = "press"
    cooldown_seconds: float = Field(default=2.0, ge=0)
    source_folder: Path = Field(default=Path("print_files"))
    switch_direction: Literal["both", "on_only", "off_only"] = "both"
```

From src/config/loader.py:
```python
class ConfigError(Exception): ...
def load_config(config_path: Path = Path("config.yaml")) -> PrinterConfig: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config watcher with watchdog, debouncing, and error resilience</name>
  <files>src/config/watcher.py, src/config/__init__.py, tests/test_config_watcher.py, requirements.txt</files>
  <behavior>
    - test_watcher_calls_callback_on_file_change: Write config file, start watcher, modify file, callback called within 2s
    - test_watcher_debounces_rapid_changes: Modify file 3 times in 0.2s, callback called only once (debounce window is 1.0s)
    - test_watcher_survives_invalid_config: Start watcher, write invalid YAML to config, watcher does NOT crash, callback receives the error or handles it gracefully
    - test_watcher_ignores_other_files: Modify a different file in same directory, callback NOT called
    - test_watcher_stop: Start watcher, call stop(), observer thread terminates cleanly
    - test_start_config_watcher_returns_observer: start_config_watcher() returns an Observer that can be stopped
  </behavior>
  <action>
    RED phase:
    1. Add `watchdog>=6.0` to requirements.txt (append with descriptive comment). Run `pip install -r requirements.txt`.
    2. Create `tests/test_config_watcher.py` with all behavior tests. Tests use tmp_path for config files. Use `time.sleep()` sparingly for async file event propagation (max 2s waits with polling). Import `ConfigWatcher, start_config_watcher` from `src.config.watcher`.
    3. Run `pytest tests/test_config_watcher.py -x` — confirm RED.

    GREEN phase:
    4. Create `src/config/watcher.py`:
       - `class ConfigWatcher(FileSystemEventHandler)`:
         - `__init__(self, config_path: Path, reload_callback: Callable)`: stores resolved path, callback, last_reload_time=0, debounce_seconds=1.0
         - `on_modified(self, event)`: check isinstance FileModifiedEvent, check path matches config_path (resolved), debounce check (time.time() - last_reload_time < debounce_seconds), call reload_callback wrapped in try/except (log error on failure, keep running)
       - `def start_config_watcher(config_path: Path, reload_callback: Callable) -> Observer`:
         - Creates ConfigWatcher, Observer
         - Schedules handler on config_path.parent (recursive=False)
         - Starts observer, returns it
    5. Update `src/config/__init__.py` to also export: `from .watcher import ConfigWatcher, start_config_watcher`
    6. Run `pytest tests/test_config_watcher.py -x` — confirm GREEN.
    7. Run `pytest tests/ -v` — confirm ALL tests green (schema + loader + watcher + v0.1).

    REFACTOR: Ensure logging uses Python logging module (not print statements) for service compatibility. Use `logging.getLogger(__name__)` in watcher.py.

    IMPORTANT: The watcher must handle the case where editors do atomic saves (write to temp file + rename). watchdog may fire on_created or on_moved instead of on_modified in some cases. Handle both FileModifiedEvent and FileClosedEvent if available, or test with actual file writes to confirm which events fire on the target platform.

    NOTE on test timing: Watcher tests involve async file events. Use a polling loop pattern: `for _ in range(20): time.sleep(0.1); if condition: break` rather than a single long sleep. This keeps tests fast when events arrive quickly but tolerant when slow.
  </action>
  <verify>
    <automated>cd /Users/lveneranta/NO_BACKUP/ducky-printer-project && pytest tests/ -v</automated>
  </verify>
  <done>All tests pass (schema, loader, watcher, and v0.1). ConfigWatcher debounces rapid file changes, survives invalid config errors, ignores unrelated files, and can be stopped cleanly. watchdog added to requirements.txt. Full config package exports load_config, ConfigError, PrinterConfig, ConfigWatcher, start_config_watcher.</done>
</task>

</tasks>

<verification>
After task completes:
1. `pytest tests/ -v` — full suite green including all v0.1 tests
2. `python -c "from src.config import load_config, ConfigError, PrinterConfig, ConfigWatcher, start_config_watcher; print('All exports OK')"` — confirms public API
3. Verify requirements.txt includes pydantic, pyyaml, and watchdog
</verification>

<success_criteria>
- Config file changes trigger reload callback within 2 seconds (CFG-07)
- Rapid saves debounced to single reload
- Invalid config during reload does not crash watcher
- Watcher lifecycle (start/stop) is clean
- All tests pass (schema + loader + watcher + existing v0.1)
- watchdog added to requirements.txt
- src/config/__init__.py exports complete public API
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-foundation/02-02-SUMMARY.md`
</output>
