---
phase: 02-configuration-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/config/__init__.py
  - src/config/schema.py
  - src/config/loader.py
  - tests/test_config_schema.py
  - tests/test_config_loader.py
  - tests/conftest.py
  - config.example.yaml
  - requirements.txt
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05
  - CFG-06

must_haves:
  truths:
    - "GPIO pin number 2-27 accepted, outside range rejected with clear error"
    - "Trigger mode only accepts 'press' or 'switch', rejects other strings"
    - "Cooldown accepts non-negative floats, rejects negative values"
    - "Source folder path validated as existing directory"
    - "Switch direction only accepts 'both', 'on_only', or 'off_only'"
    - "Missing config file returns all defaults without error"
    - "Invalid YAML syntax produces clear error message with file path"
    - "Invalid field values produce per-field error messages"
  artifacts:
    - path: "src/config/schema.py"
      provides: "PrinterConfig Pydantic dataclass with all fields and validators"
      exports: ["PrinterConfig"]
    - path: "src/config/loader.py"
      provides: "YAML loading with validation and user-friendly errors"
      exports: ["load_config", "ConfigError"]
    - path: "src/config/__init__.py"
      provides: "Public API re-exports"
      exports: ["load_config", "ConfigError", "PrinterConfig"]
    - path: "tests/test_config_schema.py"
      provides: "Schema validation unit tests for CFG-01 through CFG-05"
    - path: "tests/test_config_loader.py"
      provides: "Loader integration tests for CFG-06"
    - path: "config.example.yaml"
      provides: "Documented example config with all fields and defaults"
  key_links:
    - from: "src/config/loader.py"
      to: "src/config/schema.py"
      via: "imports PrinterConfig"
      pattern: "from .schema import PrinterConfig"
    - from: "src/config/loader.py"
      to: "yaml.safe_load"
      via: "YAML parsing (never yaml.load)"
      pattern: "yaml\\.safe_load"
    - from: "src/config/__init__.py"
      to: "src/config/loader.py"
      via: "re-exports load_config, ConfigError"
      pattern: "from .loader import"
---

<objective>
Create the configuration schema (Pydantic dataclass) and YAML loader with validation and user-friendly error messages, using TDD.

Purpose: Establishes the config contract that all subsequent phases depend on (GPIO listener reads pin/mode/cooldown, file picker reads source_folder, trigger handler reads all settings). Getting validation right here prevents bad config from propagating.

Output: `src/config/` package with schema.py + loader.py, full test coverage, example config file, updated requirements.txt.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-foundation/02-RESEARCH.md
@.planning/phases/02-configuration-foundation/02-VALIDATION.md
@requirements.txt
@pytest.ini
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config schema with Pydantic dataclass and validation tests</name>
  <files>src/config/__init__.py, src/config/schema.py, tests/test_config_schema.py, tests/conftest.py, requirements.txt</files>
  <behavior>
    - test_gpio_pin_default: PrinterConfig() has gpio_pin=17
    - test_gpio_pin_valid_range: gpio_pin=2 and gpio_pin=27 accepted
    - test_gpio_pin_too_low: gpio_pin=1 raises ValidationError
    - test_gpio_pin_too_high: gpio_pin=28 raises ValidationError
    - test_gpio_pin_reserved: gpio_pin=0, 1, 14, 15 raise ValidationError with "reserved" message
    - test_trigger_mode_default: PrinterConfig() has trigger_mode="press"
    - test_trigger_mode_valid: "press" and "switch" accepted
    - test_trigger_mode_invalid: "toggle" raises ValidationError
    - test_cooldown_default: PrinterConfig() has cooldown_seconds=2.0
    - test_cooldown_zero: cooldown_seconds=0 accepted
    - test_cooldown_negative: cooldown_seconds=-1 raises ValidationError
    - test_source_folder_valid: existing directory path accepted (use tmp_path fixture)
    - test_source_folder_nonexistent: nonexistent path raises ValidationError with "does not exist"
    - test_source_folder_is_file: path to a file raises ValidationError with "not a directory"
    - test_source_folder_default: PrinterConfig() has source_folder=Path("print_files") (NOTE: skip existence validation for default value since it may not exist at test time — add a note that existence is validated at load time, not at schema instantiation with defaults)
    - test_switch_direction_default: PrinterConfig() has switch_direction="both"
    - test_switch_direction_valid: "both", "on_only", "off_only" accepted
    - test_switch_direction_invalid: "left" raises ValidationError
    - test_all_defaults: PrinterConfig() succeeds with all default values
  </behavior>
  <action>
    RED phase:
    1. Add `pydantic>=2.0` and `pyyaml>=6.0` to requirements.txt (append after existing entries with descriptive comments). Run `pip install -r requirements.txt`.
    2. Create `src/config/__init__.py` as empty file (will populate exports in Task 2).
    3. Create `tests/test_config_schema.py` with all behavior tests listed above. Import `PrinterConfig` from `src.config.schema`. Tests MUST fail (module doesn't exist yet).
    4. Add a `tmp_config_dir` fixture to `tests/conftest.py` that creates a temp directory (for source_folder tests).
    5. Run `pytest tests/test_config_schema.py -x` — confirm RED (ImportError or test failures).

    GREEN phase:
    6. Create `src/config/schema.py` with `PrinterConfig` Pydantic dataclass:
       - `gpio_pin: int = Field(default=17, ge=2, le=27)` with `@field_validator` rejecting reserved pins {0, 1, 14, 15}
       - `trigger_mode: Literal["press", "switch"] = "press"`
       - `cooldown_seconds: float = Field(default=2.0, ge=0)`
       - `source_folder: Path = Field(default=Path("print_files"))` with `@field_validator` checking existence and is_dir (but ONLY when value differs from default, to allow schema instantiation without the folder existing)
       - `switch_direction: Literal["both", "on_only", "off_only"] = "both"`
    7. Run `pytest tests/test_config_schema.py -x` — confirm GREEN.

    REFACTOR: Review for clean code. Ensure all Field() have `description=` for documentation.

    IMPORTANT: Use `from pydantic.dataclasses import dataclass` (not BaseModel). Use `yaml.safe_load()` NEVER `yaml.load()`. Use `from typing import Literal` for enum-like fields (not Python enum).

    NOTE on source_folder validation: The `@field_validator` for source_folder should skip existence checking when the value equals the default `Path("print_files")`. This allows `PrinterConfig()` to succeed in tests and CI where the folder doesn't exist. The loader (Task 2) validates folder existence after applying config values.
  </action>
  <verify>
    <automated>cd /Users/lveneranta/NO_BACKUP/ducky-printer-project && pytest tests/test_config_schema.py -x</automated>
  </verify>
  <done>All 19 schema validation tests pass. PrinterConfig validates gpio_pin (2-27, no reserved), trigger_mode (press/switch), cooldown_seconds (>=0), source_folder (exists+is_dir), switch_direction (both/on_only/off_only). All defaults work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: YAML config loader with error formatting and defaults</name>
  <files>src/config/loader.py, src/config/__init__.py, tests/test_config_loader.py, config.example.yaml</files>
  <behavior>
    - test_load_valid_config: load_config(path_to_valid_yaml) returns PrinterConfig with values from YAML
    - test_load_missing_file_returns_defaults: load_config(nonexistent_path) returns PrinterConfig() with all defaults
    - test_load_empty_file_returns_defaults: load_config(empty_yaml) returns PrinterConfig() with all defaults
    - test_load_partial_config: YAML with only gpio_pin=22 returns config with gpio_pin=22 and all other defaults
    - test_load_invalid_yaml_syntax: load_config(malformed_yaml) raises ConfigError with file path in message
    - test_load_invalid_values: load_config(yaml_with_gpio_pin=99) raises ConfigError with field-level error message
    - test_load_multiple_errors: YAML with gpio_pin=99 AND cooldown=-1 raises ConfigError listing both errors
    - test_config_error_includes_field_names: ConfigError message contains the failing field name
    - test_config_error_is_human_readable: ConfigError message does NOT contain Python tracebacks or Pydantic internals
    - test_load_validates_source_folder_exists: YAML with source_folder pointing to nonexistent dir raises ConfigError
  </behavior>
  <action>
    RED phase:
    1. Create `tests/test_config_loader.py` with all behavior tests. Import `load_config, ConfigError` from `src.config.loader`. Use tmp_path to create temp YAML files for each test. Run `pytest tests/test_config_loader.py -x` — confirm RED.

    GREEN phase:
    2. Create `src/config/loader.py`:
       - `class ConfigError(Exception)` for user-friendly config errors
       - `def load_config(config_path: Path = Path("config.yaml")) -> PrinterConfig`:
         - Missing file: return `PrinterConfig()` (all defaults, skip source_folder existence check)
         - Open and parse with `yaml.safe_load(f) or {}` (handle empty file)
         - Validate with `PrinterConfig(**data)`
         - Catch `yaml.YAMLError` → raise `ConfigError` with file path and syntax details
         - Catch `ValidationError` → format each error as `"  - {field}: {message}"`, raise `ConfigError` with formatted list
         - For source_folder: after Pydantic validation passes, explicitly check `config.source_folder.exists()` and `.is_dir()` — raise ConfigError if not
    3. Update `src/config/__init__.py` to export: `from .schema import PrinterConfig`, `from .loader import load_config, ConfigError`
    4. Run `pytest tests/test_config_loader.py -x` — confirm GREEN.
    5. Run `pytest tests/test_config_schema.py tests/test_config_loader.py -x` — confirm all still GREEN.

    REFACTOR: Ensure error messages are clear for Pi users (no jargon).

    6. Create `config.example.yaml` in project root with all fields documented:
       ```yaml
       # Ducky Printer Configuration
       # Copy to config.yaml and edit as needed.
       # All values shown are defaults — omit any line to use the default.

       # GPIO pin number (BCM numbering, 2-27)
       gpio_pin: 17

       # Trigger mode: "press" (momentary button) or "switch" (toggle switch)
       trigger_mode: press

       # Minimum seconds between print activations (0 = no cooldown)
       cooldown_seconds: 2.0

       # Folder containing files to print (.txt, .png, .jpg, .bmp)
       source_folder: print_files

       # Switch trigger direction: "both", "on_only", or "off_only"
       # Only used when trigger_mode is "switch"
       switch_direction: both
       ```

    IMPORTANT: Use `yaml.safe_load()` — NEVER `yaml.load()`.
  </action>
  <verify>
    <automated>cd /Users/lveneranta/NO_BACKUP/ducky-printer-project && pytest tests/test_config_schema.py tests/test_config_loader.py -x</automated>
  </verify>
  <done>All schema and loader tests pass. load_config returns validated PrinterConfig from YAML, returns defaults for missing/empty files, raises ConfigError with human-readable per-field messages for invalid configs. config.example.yaml documents all settings.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pytest tests/test_config_schema.py tests/test_config_loader.py -v` — all tests green
2. `python -c "from src.config import load_config, ConfigError, PrinterConfig; print(load_config())"` — prints default config
3. `pytest tests/ -v` — full suite including v0.1 tests still passes
</verification>

<success_criteria>
- PrinterConfig Pydantic dataclass validates all 5 config fields (CFG-01 through CFG-05)
- load_config() loads YAML, applies defaults for missing values, returns validated config
- Invalid config raises ConfigError with per-field human-readable error messages (CFG-06)
- config.example.yaml documents all settings with comments
- All existing v0.1 tests remain green
- pydantic and pyyaml added to requirements.txt
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-foundation/02-01-SUMMARY.md`
</output>
