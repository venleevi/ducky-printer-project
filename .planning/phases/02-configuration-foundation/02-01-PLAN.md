---
phase: 02-configuration-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - tests/test_config.py
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05

must_haves:
  truths:
    - "System can load YAML configuration files"
    - "Configuration validates GPIO pin ranges"
    - "Configuration provides boolean enable flags for triggers"
    - "Configuration specifies target file path"
  artifacts:
    - path: "requirements.txt"
      provides: "PyYAML dependency declaration"
      contains: "pyyaml"
    - path: "tests/test_config.py"
      provides: "Configuration loading and validation test suite"
      min_lines: 100
      exports: ["test_load_valid_config", "test_gpio_pin_validation", "test_missing_config_file"]
  key_links:
    - from: "tests/test_config.py"
      to: "src/config.py"
      via: "import statement"
      pattern: "from src.config import"
---

<objective>
Establish PyYAML dependency and create comprehensive test scaffold for configuration loading and validation.

Purpose: Enable TDD development of config module by providing dependency and test infrastructure first
Output: Updated requirements.txt with PyYAML, complete test_config.py with test stubs for all CFG requirements
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-foundation/02-RESEARCH.md

# Prior work from v0.1
@.planning/phases/01-print-files-from-usb-folder/01-01-SUMMARY.md

# Test infrastructure context
@tests/conftest.py
@tests/test_file_handler.py
</context>

<interfaces>
<!-- Existing test patterns to follow -->

From tests/conftest.py:
```python
import pytest
from pathlib import Path
from unittest.mock import MagicMock

@pytest.fixture
def mock_printer():
    """Returns mock python-escpos Usb printer instance."""
    printer = MagicMock()
    # ... setup
    return printer
```

From tests/test_file_handler.py:
```python
import pytest
from pathlib import Path
from src.file_handler import read_file, resolve_filepath, FileError

def test_read_valid_utf8_file(tmp_path):
    """Test reading valid UTF-8 text file."""
    # ... test implementation
```

Key patterns:
- Use pytest fixtures for test data and mocks
- Use tmp_path fixture for temporary file creation
- Test both success cases and error cases
- Clear docstrings explaining what each test validates
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add PyYAML dependency</name>
  <files>requirements.txt</files>
  <action>
    Add PyYAML 6.0.3+ to requirements.txt for YAML configuration file parsing. According to RESEARCH.md, PyYAML 6.0.3 is the current stable version with Python 3.13 compatibility.

    Add this line to requirements.txt:
    ```
    # YAML configuration file parsing
    # Provides safe_load() for secure YAML parsing without arbitrary code execution
    pyyaml>=6.0.3
    ```

    Place after pyusb dependency, before pytest section (maintaining logical grouping: printer libs, then test libs).

    Why PyYAML: De facto standard for Python YAML parsing, stdlib dataclasses provide validation (no need for heavier Pydantic dependency).
  </action>
  <verify>
    <automated>grep "pyyaml>=6.0.3" requirements.txt</automated>
  </verify>
  <done>requirements.txt contains pyyaml>=6.0.3 dependency declaration</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create configuration test scaffold</name>
  <files>tests/test_config.py</files>
  <behavior>
    Tests cover all CFG-01 through CFG-05 requirements:

    CFG-01 (System reads YAML):
    - test_load_valid_config: Loads YAML file and returns PrinterConfig object
    - test_missing_config_file: FileNotFoundError with clear message listing search paths
    - test_invalid_yaml_syntax: ValueError with line/column information
    - test_empty_config_file: Loads empty YAML, uses all default values

    CFG-02/CFG-03 (Enable flags):
    - test_gpio_enabled_flag: Reads gpio_enabled boolean from YAML
    - test_web_enabled_flag: Reads web_enabled boolean from YAML
    - test_partial_config_uses_defaults: Missing flags use True defaults

    CFG-04 (Target file path):
    - test_target_file_path: Reads target_file from YAML as string
    - test_path_conversion: Converts YAML string to pathlib.Path object

    CFG-05 (GPIO pin):
    - test_gpio_pin_number: Reads gpio_pin integer from YAML
    - test_invalid_gpio_pin_range: ValueError for pins outside 0-27 (BCM range)
    - test_gpio_pin_type_validation: TypeError for non-integer pin values

    Each test should be a stub that creates minimal YAML fixture using tmp_path, calls load_config(), and includes a pytest.skip("Test stub - implement in Wave 2") marker.
  </behavior>
  <action>
    Create tests/test_config.py with comprehensive test stubs for configuration loading and validation. Follow existing test patterns from test_file_handler.py (pytest fixtures, tmp_path usage, clear docstrings).

    Structure:
    1. Module docstring explaining configuration test coverage
    2. Import statements (pytest, pathlib.Path, src.config imports)
    3. 12 test functions covering all CFG requirements (see behavior section)

    Each test stub should:
    - Have descriptive docstring explaining what it validates
    - Create minimal YAML test fixture using tmp_path
    - Call load_config() or PrinterConfig() constructor
    - Include pytest.skip("Test stub - implement in Wave 2") as first line after docstring
    - Show expected behavior in comments (what assertions will be made)

    Follow RESEARCH.md patterns:
    - Use tmp_path.write_text() for YAML fixture creation
    - Test both success and error cases (FileNotFoundError, ValueError, TypeError)
    - Validate GPIO pin range (0-27 for Raspberry Pi 3B+ BCM numbering)
    - Check YAML edge cases (empty file, invalid syntax, type mismatches)

    Reference existing test_file_handler.py for pytest style and fixture usage.
  </action>
  <verify>
    <automated>pytest tests/test_config.py -v --collect-only | grep -c "test_" | grep -E "^12$"</automated>
    <note>Wave 1 verification checks test structure only (12 tests collect successfully). Tests are stubs with pytest.skip() markers. Behavioral verification (tests actually pass) occurs in Wave 2 (plan 02-02).</note>
  </verify>
  <done>tests/test_config.py exists with 12 test stubs covering all CFG-01 through CFG-05 requirements, following project pytest patterns</done>
</task>

</tasks>

<verification>
1. Run `pytest tests/test_config.py -v` - all tests should be skipped (stubs)
2. Check `cat requirements.txt | grep pyyaml` - dependency declared
3. Verify test coverage: `pytest tests/test_config.py --collect-only` shows 12 tests
4. Check test file structure matches test_file_handler.py patterns
</verification>

<success_criteria>
- requirements.txt declares pyyaml>=6.0.3
- tests/test_config.py exists with 12 test stubs
- All test stubs are marked with pytest.skip() and have clear docstrings
- Test file follows project pytest patterns (tmp_path fixtures, clear naming)
- Running pytest tests/test_config.py shows 12 skipped tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-foundation/02-01-SUMMARY.md`
</output>
