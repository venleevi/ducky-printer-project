# Thermal printer ESC/POS control library
# Provides USB printer communication, command generation, and image processing
python-escpos>=3.0

# USB backend for python-escpos (explicitly listed for reliability)
pyusb>=1.0

# Testing framework for automated unit and integration tests
pytest>=9.0

# Mock object library for pytest - allows mocking USB hardware in tests
pytest-mock>=3.14

# Data validation with type hints and runtime checking
pydantic>=2.0

# YAML parsing for configuration files (use safe_load for security)
pyyaml>=6.0

# Filesystem event monitoring for config hot-reload
watchdog>=6.0
